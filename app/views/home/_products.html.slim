.row style="max-width: 85rem"
  - if @products.count >= 36
    - @products.in_groups(3).to_a.each_with_index do |products, i|
      - products.each do |product|
        .columns.medium-4.small-6.large-3 style="margin-bottom: 46px" class="js-product-group #{i == 0 && showFirst ? '' : 'hidden-products'} js-product-id-#{product.id}"
          = render partial: 'home/product_fields', locals: {product: product}

  - else
    - @products.each do |product|
      .columns.medium-4.small-6.large-3 style="margin-bottom: 46px" class="js-product-group js-product-id-#{product.id}"
        = render partial: 'home/product_fields', locals: {product: product}
